# Deny direct web access to application internals.
# (The real document root should be the /public folder.)
<FilesMatch "\.(env|md|sql)$">
  Require all denied
</FilesMatch>
RedirectMatch 403 ^/?(app|config|storage|database|cron|design)(/|$)
